# スマホ閲覧アプリ

スマホ画面: `/mobile`

## 同一Wi-Fiで接続

```bash
cd iot-market-ui
npm run dev -- --host 0.0.0.0 --port 5173
```

スマホで:

```txt
http://<PCのLAN_IP>:5173/mobile
```

## 注意

- 購入時は MetaMask モバイルのブラウザを推奨
