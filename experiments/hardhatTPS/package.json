{
  "name": "hardhat-tps-benchmark",
  "version": "1.0.0",
  "description": "Experiment: User Number vs Throughput (on-chain only)",
  "scripts": {
    "compile": "npx hardhat compile",
    "node": "npx hardhat node",
    "benchmark": "npx hardhat run scripts/benchmark.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "@types/node": "^20.11.17",
    "hardhat": "^2.28.6"
  },
  "dependencies": {
    "dotenv": "^16.4.1"
  }
}
